# v0.7.5 — Fix sidebar footer action slot for DSH 0.1.2-rc.1

## Исправлено / Fixed

- **#86 Регистрация QuickQrPopover переведена в слот `sidebar.footer.action`**. В ядре DSH `0.1.2-rc.1` контракт слотов боковой панели (`@deepseek-ai/dsh-client-ui-sidebar`) объявляет слот подвала как `sidebar.footer.action`, а не `sidebar.footer`.
- Глухие блоки `catch (_) {}` при регистрации слотов в `lib/client.js` заменены на логирование предупреждений с контекстом ошибки (`console.warn`), что исключает скрытые сбои регистрации интерфейсных компонентов.
- В `test/access.test.mjs` исправлены адреса проверки правил для подсети `192.168.77.0/24`.
- Обновлен тест `test/quick-access-pwa.test.mjs` для проверки слота `sidebar.footer.action`.